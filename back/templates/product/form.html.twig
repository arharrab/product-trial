{% extends 'layouts/base_admin.html.twig' %}

{% block body %}
    <h1>{{ product is defined ? 'Modifier le produit' : 'Nouveau produit' }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
